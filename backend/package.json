{
  "name": "eco-herd-telemetry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.14.0" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test"
  }
}
